.my-block-content-block
  .grid.grid--flush.grid--center
    - view_model.series.each_with_index do |image, i|
      - unless image.asset.image_placeholder?
        .grid__cell.grid__cell--50-at-medium
          - if image.link.present?
            = link_to image.link, class: 'my-block-content-block__link' do
              = image_tag image.asset.url, alt: image.alt, class: 'my-block-content-block__image'
          - else
            = image_tag image.asset.url, alt: image.alt, class: 'my-block-content-block__image'
