module Workarea
  decorate Pricing::Price, with: :b2b do
    decorated do
      field :price_list_id, type: String
    end

    def generic?
      super && price_list_id.blank?
    end

    def generic_for_price_list?
      min_quantity == 1 && price_list_id.present?
    end
  end
end
