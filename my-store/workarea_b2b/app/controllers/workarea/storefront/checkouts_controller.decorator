module Workarea
  decorate Storefront::CheckoutsController, with: :b2b do
    decorated do
      before_action :set_checkout_account, except: [:confirmation]
    end

    private

    def set_checkout_account
      return unless current_account.present?
      current_order.account_id = current_account.id
    end
  end
end
